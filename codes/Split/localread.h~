#ifndef __LOCALREAD_H
#define __LOCALREAD_H
#include"Iread.h"
#include<fstream>
class LocalRead:public IRead
{
fstream f;
	public: long int fileopen(char fname[]);
		void fileread(char fname[],int csize);
		void fileclose();

};
#endif
