#include"Iwrite.h"
#include<fstream>
#include<iostream>

class LocalWrite:public IWrite
{
	public: void fileopen(char[],fstream *f);
	        void filewrite(char[],fstream *f,int csize);
		int fileclose(fstream *f);

LocalWrite()
{

}

};
